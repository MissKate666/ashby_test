import { useEffect, useRef } from 'react';import { drawDiagram } from './useDiagram';import { useResizeObserver } from '../../hooks/useResizeObserver';import './diagramStyles.css';
export default function AshbyDiagram({data,setParams}){const wrap=useRef(null),svg=useRef(null),api=useRef(null);const size=useResizeObserver(wrap);useEffect(()=>{api.current=drawDiagram(svg.current,data,size,(intercept)=>setParams(p=>({...p,intercept})))},[data,size.width,size.height,setParams]);return <div ref={wrap} className="diagram panel relative h-full min-h-[55vh]"><div id="tooltip" className="tooltip hidden"/><div className="absolute right-3 top-3 z-10 flex gap-2"><button className="btn bg-white border" onClick={()=>api.current?.zoomBy(1.25)}>+</button><button className="btn bg-white border" onClick={()=>api.current?.zoomBy(.8)}>-</button></div><svg ref={svg}/></div>}
